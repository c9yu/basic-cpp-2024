// date : 20240223
// file : test14FileDivision.cpp
// desc : 파일 분할
/*
#include <iostream>

class AClass {
private: // 접근 제어 지시자를 멤버 변수에서도 쓰고, (그러나 private의 경우 디폴트 값으로 들어가 있다.)
	int num;

public: // 접근 제어 지시자 멤버 함수에서도 써야한다.
	AClass(int anum) { // 생성자이다. 생성자: 초기화 담당
		num = anum;
	}
	void Ainfo() {
		std::cout << "A::num: " << num << std::endl;
	}
};
int main()
{
	AClass a(10);
	a.Ainfo();

	return 0;
}
*/