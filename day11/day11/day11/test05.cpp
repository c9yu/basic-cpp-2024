/*
#include <iostream>
using namespace std;

int main()
{
	
	int code = 97;
	cout << code << endl;
	cout << (char)code << endl;
	cout << "===========================================" << endl;
	char ary[5] = { 'a', 'b', 'c','d','e' }; // 문자열 타입의 배열 사전
	cout << ary << endl;
	printf("==========================================="); // 널문자 - 문자열의 끝을 알린다.
	char ary1[6] = { 'a', 'b', 'c','\n','e' };
	cout << ary1 << endl;
	cout << sizeof(ary1) << endl;   // 전체 문자 크기
	cout << sizeof(ary1) << endl;   // 널문자 앞까지의 크기
	printf("===========================================");
	char str[] = "string"; // 자동으로 널문자가 붙는다.
	cout << str << endl;
	cout << sizeof(str) << endl; // 널문자가 포함된 크기가 리턴된다.
	cout << sizeof(str) << endl;
	printf("===========================================");
	const char* pstr = "STRING";
	cout << pstr << endl;
	cout << sizeof(str) << endl; // 널문자가 포함된 크기가 리턴된다.
	cout << strlen(str) << endl;
	
	//pstr[0] = 's' // 문자열을 포인터로 사용하는 경우는 변경이 불가능하다.
	
	printf("===========================================");
	printf("===========================================");
*/

/*
	string s = "string";
	cout << s << endl;
	cout << sizeof(s) << endl;
	cout << s.size() << endl;

	string s1 = "Hongkildon";
	cout << s1 << endl;
	cout << sizeof(s1) << endl;
	cout << s1.size() << endl;

	return 0;
}
*/