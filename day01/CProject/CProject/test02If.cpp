// date : 20240221 
// file : testIf02.cpp
// desc : 선택 제어문 : if문

/*
#include <iostream>

using namespace std;

int main() {

	int inkey;
	cout << "정수 입력 > " << endl;
	cin >> inkey;

	if (inkey == 3) { // if(조건식){... 실행문}
		cout << "빙고" << endl;
	}

	else if (inkey > 3) { // python의 elif이지만, c++에서는 else if가 된다.
		cout << "숫자가 큽니다." << endl;
	}

	else if (inkey < 3) {
		cout << "숫자가 작습니다." << endl;
	}
	else cout << "잘못 입력하셨습니다."; // if도 아니고, else if도 아닐때 전부 else에 해당된다.

	return 0;
}
*/