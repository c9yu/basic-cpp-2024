// date : 20240221 
// file : test04For.cpp
// desc : 선택 제어문 : switch문

/*
using namespace std;

#include <iostream>

int main() {

	int sum = 0; // sum 값을 0으로 초기화 한다. 누적되는 값을 저장할 때는 반드시 초기화를 해주고 사용한다.
	for (int i = 1; i <= 100; i++) {
		sum += i; // sum 값을 0으로 초기화를 시켜두지 않으면, i와 뭘 더할지 모르므로 오류가 발생한다.
	}
	
	cout << sum << endl;

	return 0;
		 
}
*/