// date : 2024-03-11
// file : test01.cpp
// desc :

/*
#include <iostream>

using namespace std;

int Result(int _n1, int _n2) {
	if (!_n2) throw _n2;
	cout << "+: " << _n1 + _n2 << endl;
	cout << "-: " << _n1 - _n2 << endl;
	cout << "X: " << _n1 * _n2 << endl;
	cout << "/: " << _n1 / _n2 << endl;
}


int main(int argc, char* argv[])
{
	int n1, n2;
	cout << "정수 2개를 입력하세요: ";
	cin >> n1 >> n2;
	try {
		Result(n1, n2);
	}
	catch (int exception) {
		cout << "0으로 나눌 수 없습니다." << endl;
	}

	int Result();

	return 0;
}

// vector - 자동으로 메모리가 할당, template
// v.front(), v.back(), push_back(), pop_back()
// <vactor>라는 헤더파일이 필요
*/